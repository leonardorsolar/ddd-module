import Repo from '../interfaces/__TEMPLATE__FILE__NAME__-repo.interface';

export const repoMock: Repo = {
	create: jest.fn(),
	update: jest.fn(),
	findById: jest.fn(),
	deleteById: jest.fn(),
	findMany: jest.fn(),
}

export default repoMock;
